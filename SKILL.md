---
name: deep-research-cn
description: 中文全网深度调研并产出结构化报告。当用户要求"调研"、"研究报告"、"全网查"、"查一下"、"市场分析"、"竞品分析"、"技术选型"、"看看大家怎么评价 X"等任务时使用。提供多源检索路由、交叉验证、口径标注、数据成表、来源清单的完整管线，并适配沙箱无直连网络的运行环境。Use for Chinese deep-research tasks such as market analysis, competitor research, and technology evaluation.
---

# 中文深度调研管线

把"全网查资料"变成一次**可复现、带来源、口径清晰**的调研。
核心纪律只有一条：**每一个关键结论都必须能溯源**。宁可写"未找到公开数据"，也不编造数字。

## 总流程

```
定题拆解 → 多源检索 → 交叉验证 → 数据成表 → 结构化成稿 → 来源清单
```

按顺序执行六步；每一步的具体规则在下方，深入细节再按需读取对应 references 文件。

## 第一步：定题拆解

先明确四件事，再动手检索。信息不足时直接问用户，不要猜：

1. **范围**：调研对象的边界（行业/产品/时间窗，如"2025-2026 年中国微短剧市场"）。
2. **产出类型**：市场调研 / 竞品分析 / 技术选型，对应 `templates/` 下三份模板之一。
   用户没指定时，按任务语义自选并在开头声明"本报告采用 XX 模板"。
3. **读者与用途**：给谁看、用来做什么决策。这决定深度和详略取舍。
4. **拆解子问题**：把主题拆成 3-6 个可检索的子问题（如市场规模、玩家格局、
   商业模式、趋势风险）。每个子问题独立检索，最后汇总成稿。

## 第二步：多源检索

**先读 `references/search-routing.md` 了解信息源分层与查询策略**，要点：

- 按层级检索：官方权威源 → 行业研究机构 → 主流媒体 → 社区讨论 → 英文补充源。
- 中英双语检索；用数字和口径词细化查询（如"市场规模 亿元"、"MAU"、"同比增长"）。
- 每个子问题至少命中 2 个**相互独立**的来源（转载、引用同一原始报告不算独立）。
- 边检索边记录：来源名、标题、日期、URL、关键数据原文。**不要凭记忆引用**。

沙箱环境（Bash 无直连网络）的降级策略见 `references/sandbox-notes.md`：
优先走 MCP 检索工具，网页读不动时用 Reader 类服务兜底，**绝不因为抓不到原文就放弃标注来源**。

## 第三步：交叉验证

**规则详见 `references/verification.md`**，要点：

- 关键数字必须有 ≥2 个独立来源佐证，或在报告中明确标注"单一来源，未经交叉验证"。
- 不同机构数字冲突是常态（统计口径不同），**处理方式是并列呈现 + 标注口径，
  而不是擅自选一个或取平均**。
- 引用数据时区分：官方统计 / 机构估算 / 媒体转述 / 社区 anecdote，并在文中注明可信层级。

## 第四步：数据成表

数字一律进表格，不散落在段落里。每张数据表必须有：

1. **口径列或口径脚注**：统计范围、时间点、是否含细分项。
2. **来源编号**：对应文末来源清单的 `[n]`。
3. 时间序列尽量成列，方便读者看趋势而不是孤立单点。

## 第五步：结构化成稿

按 `templates/` 对应模板成稿。共性要求：

- 开头 3-5 条**核心结论**，每条结论后面跟来源编号，让读者 30 秒拿到重点。
- 正文先结论后论据；每个小节回答一个子问题。
- 明确区分"事实（有来源）"与"推断（分析）"。推断性内容标注"（推断）"。
- 中文写作，机构名保留原文，英文术语首次出现给出中文对照。

## 第六步：来源清单与链接核查

文末附编号来源清单，格式：

```
[1] 来源机构《文章标题》发布日期. URL
```

- 来源按首次引用顺序编号；同一来源多次引用复用同一编号。
- 有网络和 Python 环境时，运行 `python scripts/check_links.py 报告.md`
  批量核查链接可达性（纯标准库，无第三方依赖）。
- 沙箱环境跳过此步，改为在交付说明中提醒读者"链接可达性未在线核查"。
